spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

rabbit:
  stock:
    exchange: test.stock.exchange
    queue: test.stock.queue
    routing-key: stock.*

rabbitmq:
  queue:
    order-created: "order-queue"